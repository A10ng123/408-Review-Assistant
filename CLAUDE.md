# CLAUDE.md — 408 考研复习助手项目指引

## 项目概述

这是一个面向 408 计算机考研的复习助手工具，纯前端网页应用（HTML + CSS + Vanilla JS），双击 `index.html` 即可在浏览器中运行。用户为编程初学者，本项目也是其学习编程的实践项目。

## 工作原则

- **小步推进**：每阶段只改 2-4 个文件，完成后请用户验证再继续
- **代码可读性优先**：中文注释关键逻辑，变量命名清晰，不追求花哨写法
- **零依赖优先**：能用纯 CSS/JS 实现的不用第三方库，保持项目简单
- **数据安全**：所有数据仅存 localStorage，不做网络请求

## 关键文件路径

| 文件 | 用途 |
|------|------|
| `docs/requirements.md` | 需求规格说明，定义所有功能和非功能需求 |
| `docs/tech-spec.md` | 技术规格说明，数据模型、SM-2 算法、存储规范 |
| `docs/design-spec.md` | 设计规范，色彩系统、布局结构、交互规范 |
| `docs/development-plan.md` | 开发执行计划，分 7 个阶段，每阶段有目标和验证标准 |
| `devlog/` | 开发日志文件夹，每天开发完成后记录 |

## 开发流程

1. 参考 `docs/development-plan.md` 确定当前阶段
2. 参考 `docs/tech-spec.md` 了解数据模型和技术约束
3. 参考 `docs/design-spec.md` 确保 UI 一致
4. 代码完成后，按该阶段的"验证"标准自测
5. 在 `devlog/` 中更新当日开发日志（按日期命名，如 `2026-05-19.md`）

## 用户约定

- 用户需要预置 408 完整考纲知识点（数据结构、计组、操作系统、计网）
- 用户偏好简洁直接的交互，不需要冗余的确认弹窗
- 功能优先于美观，但不牺牲基本可用性

## 技术约束

- 纯静态文件，不引入任何构建工具（webpack/vite 等）
- 不使用任何 JS 框架（React/Vue 等）
- 兼容 Chrome、Safari、Edge、Firefox 主流浏览器
- 数据模型和 localStorage 键名严格遵守 `docs/tech-spec.md` 中的定义
